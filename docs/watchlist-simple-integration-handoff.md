# Selectable Simple analysis — release handoff

Owner authorized completion and deployment September 14. Source integration
complete; hosted compilation, deployment and acceptance remain mandatory.

## Behavior

- Analysis Review has Current analysis / Simple analysis. Missing saved setting
  defaults Current. Existing reads keep their format. Simple always requires
  durable owner review when generation/session are on; generation-off sessions
  keep the existing publishing behavior. No pending draft is approved by a switch.
- Format is captured at generation start before awaited preparation. Simple uses
  its separate tested prompt/schema, selected model/effort, raw available daily,
  4h, full-session 5m and recent 1m evidence. No web-search tool or paid retry.
- Saved simpleAnalysis is the authoritative four-section body. Legacy summary,
  upper boundary and failure fields are compatibility projections only. Do not
  reinterpret missing legacy pullbackPlans as an absent Simple pullback.
- Both editors preserve numeric areas, text, optional invalidation and hide
  controls. Original responses, prepared versions and edits remain separate.
  Discord preview/publication use the same simple-format formatter.
- Current prompts no longer request dilution/listing research; outgoing schemas
  omit those fields, stored historical fields remain compatible. Both formats'
  Discord bodies omit those unavailable standalone research sections.
- Hidden live-five-minute card remains disconnected. Web-search setting is not
  changed or enabled. No migration or hosting relocation.

## Verification

- 18 focused simple/input/research tests passed before adding the actual manager
  admission test; the final 3-test simple integration suite passes separately.
- Existing review API/panel, policy/store, HTTP/Discord publication gates and
  publication preview passed. Settings expectations updated for Current default.
- Current service, settings, review API and simple integration rerun passed after
  updating pinned prompt/schema hashes for the owner-requested research removal.
- Narrow strict TypeScript check on simple-content/settings passed.
- One full no-emit check exhausted the imposed 384 MB heap. Not retried at higher
  memory. Hosted full compilation/type checking is a required release gate.
- Platform has four passing source/render checks for parsing compatibility,
  hidden sections, range editing, escaping and changed TSX syntax.
- No paid requests, public approval or Discord sends during integration checks.
  Earlier seven-symbol Luna/Terra comparisons remain in the prototype progress.

## Release and rollback

Coordinator reported runtime parent 4f795dccec2455421610db6c0dd727c3da139be7
and deployment 7e833018-1fdd-4d33-be3a-f15608f6593f healthy; freshly verify remote.
Platform parent must be freshly discovered. Use exact file allowlists, not a
historical range cherry-pick. Include the local 4h price-action and market-context
dependencies absent from that runtime parent. Publish Platform support FIRST,
then runtime. Keep Current selected until both are healthy.

Preserve the existing staging-named runtime connection. Do not change provider,
search, approval/session, automatic-update or budget settings as part of release.
Verify actual hosted search remains OFF; report rather than silently enabling.

Rollback new-request selection to Current without deleting saved Simple drafts.
Keep Platform simple parsing/render support when rolling runtime back so saved
Simple reads remain viewable. Never replay uncertain Discord deliveries.

Hosted acceptance: load/save/reload selector, verify Current and old drafts,
generate one owner-private Simple draft with search OFF, verify request packet
timeframe coverage and no tools, view/edit/save/reopen/hide/ranges, exact website
and Discord preview, pending public concealment, health. No public/Discord test
post is needed to verify a held draft. Use fake transport gate evidence for send
authorization unless a separately coordinated real publication test is chosen.
