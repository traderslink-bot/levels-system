# Simple analysis alternative

Current checkpoint record: [Simple analysis progress](watchlist-simple-analysis-progress.md).

Owner approved selectable integration on September 14 after the separate
seven-symbol Luna/Terra comparison: "Yes a selectable option is a good idea."
This supersedes the prototype-only boundary. Preserve Current analysis, saved
reads, existing review/edit/approval controls and the existing hosting layout.

## Approved integration checkpoint

Implementation and release gates: [Selectable integration handoff](watchlist-simple-integration-handoff.md).

September 14 owner follow-up: remove dilution/listing research requests from
both Current-analysis prompt variants as well as their outgoing schema fields.
Simple already excludes them. Preserve catalyst/news input and existing source
history; optional web-search capability is not itself evidence of paid searches.
No live external-research setting change or historical billing claim is included.
Verification: [Research scope progress](watchlist-research-scope-progress.md).

- Analysis format: Current analysis / Simple analysis, default Current analysis.
- Selection affects new requests only. Capture the format before asynchronous
  data retrieval and retain it with the generation, original response and edits.
- Simple analysis has only setup, pullbacks, upside areas and thesis invalidation.
  Preserve numeric ranges, the sole meaningful pullback, and owner-hidden sections.
- Use the tested separate prompt/schema and full available candle packet. One
  request with the selected model; no automatic second-model retry or web search.
- Simple requests require a durable owner review before first public admission.
  No setting change can publish a pending draft. Generation-disabled sessions
  keep their existing behavior. Current-format follow-up settings are preserved.
- Original, prepared and edited versions stay separate in the existing audit.
  Technical parsing cannot silently delete a valid area for trading judgments.
- Wire the actual editor and saved-format renderer before exposing the selector.
  Verify ranges, hide/save/reopen, preview, approval and Discord output together.
- Reconcile the missing hosted four-hour packet path in the exact release
  allowlist. No hosting migration, publisher replacement or unrelated changes.
- Focused fake-provider checks first; paid hosted acceptance remains within the
  owner's $10 daily guard. Coordinator owns serialized release and health checks.
- Update Watchlist Help with format selection and the unchanged approval flow.

The earlier contract below is historical; the four-section contract above governs.

Current owner scope: four visible sections — overview, pullbacks, upside areas,
thesis invalidation. The later chart-first clarification removed the separate
momentum trigger; useful breakout conditions belong in the upside prose. All
earlier five-section and optional-recovery references below are historical and
superseded. Existing recovery code and saved reads remain untouched. Today's
owner-authorized testing ceiling is $10, superseding the earlier $5 references.

## Earlier selectable-live-format contract (integration deferred during prototype testing)

- Admin selector: Analysis format; options Current analysis / Simple analysis.
- Existing saved settings and reads default to Current analysis. Selection affects
  future requests only; capture it at generation start so changing the selector
  cannot relabel an in-flight or saved read.
- Simple schema asks only for current setup, one or two meaningful pullbacks,
  momentum trigger, a few significant upside areas, thesis invalidation, optional
  recovery, and brief relevant catalyst context. No must-clear, separate hold,
  caution, downside ladder, listing research or dilution research sections.
- Separate prompt/schema, same market packet and model. Exactly one request.
  Preserve complete editable drafts and approval/publication gate. Initially
  simple generation requires the existing owner-review workflow; never bypass it.
- Compatible stored payload retains existing field shapes with empty unused
  fields, plus captured format metadata. Separate presentation selects by saved
  metadata, not current settings. Old card and records remain usable.
- Original response, prepared read and owner edits remain auditable. No automatic
  rewrite of saved reads and no schema/database migration.
- Upside supplement adds one actual farther resistance when needed; never a
  ladder of all minor levels. No clock times in public prose.
- Verify format persistence, request selection, in-flight capture, one-call
  behavior, omitted legacy fields, saved format parsing, editing and display.
- Paid checks against all five packets must remain inside the daily $5 ceiling.
- Coordinator owns hosted reconciliation and release. Source tests do not prove
  hosted acceptance. Do not copy unrelated canonical runtime changes.

## Progress

- [x] Owner-approved scope recorded; contract reviewed for backward compatibility,
  single-request cost, history preservation and review gate.
- [x] Separate generation contract and focused tests implemented.
- [ ] Persistent selection, captured payload identity and admin control.
- [ ] Second card presentation and edit/preview compatibility.
- [ ] Five-symbol quality check and release handoff.

## First real five-symbol experiment — failed quality acceptance

All five completed one Luna request each, using identical saved September 14
market inputs to the earlier comparison. Each returned exactly the five sections,
no recovery, and no candle clock times. There were no live card changes, owner
approvals, Discord posts or production generation requests.

- SOAR: again selected 0.2371–0.2457 as near pullback at 0.2482 reference.
  Deeper zone 0.2288–0.2329. Four grouped upside areas through 0.343–0.356.
  Presentation reduced repetition but did not solve the near-shelf selection.
- BMGL: one 6.83–7.18 pullback and four upside areas through 10.60–11.09.
  Invalidation at 4.58 describes total session unwind, inadequately separates
  failure of the actionable plan from its oldest origin.
- FTFT: 4.67–4.76 and 4.44–4.515 selected; loses the useful earlier 4.03–4.32
  base. Upside grouping too wide (5.52–6.28 and 6.96–8.32).
- NCT: near-shelf issue persists. Adds 3.57–4.67 and 4.93–6.16 historical bands
  to a 0.452 reference; numerically observed does not make them practical
  same-day continuation areas. Also calls 0.48 the regular-session high despite
  the opening range, and uses the article subscription price to justify failure.
  Do not infer a corporate-action adjustment without evidence; the historical
  discontinuity and relevance must be handled explicitly in input/selection.
- ELMT: near 22.00–22.40 zone at 22.52 again; deeper 20.75–21.40 remains.
  Upside ends at 25.03 and does not reach requested broad coverage. Invalidation
  16.19 is the oldest session origin rather than sufficiently explained active
  thesis failure.

Batch estimated cost $0.338923. Combined known daily cost $3.7148061 plus the
previously unpriced hosted timeout; conservative hosted reserve gives $3.9821496.
Private requests/responses/assessments remain in ignored runtime data. No further
paid requests in this batch. Prototype is NOT release-ready. Next correction
must address structural selection and relevant historical context, not merely
reduce fields or hide flawed output. Preserve this baseline for comparison.

## Input-label investigation and second bounded test

Actual saved SOAR three-body window for 0.2371–0.2457 has no shared intersection:
body ranges 0.2376–0.2399, 0.2371–0.2437, 0.2433–0.2457. Labeling the union an
accepted base biases the model. Similar directional windows exist for FTFT/NCT.
Prototype-only `watchlist-simple-input.ts` preserves raw candles, reclassifies
these candidate observations, removes the recommended-candidate naming, and
flags large daily close discontinuities with cause/adjustment explicitly unknown.
No live generator changes. Three focused tests pass, including source immutability.

Repeated all five paid requests once on the same saved inputs. All completed.
- SOAR: distinct deeper 0.224–0.2323 retained; three upside areas. First area
  0.2386–0.2437 is still near reference: pullback role problem not solved.
- BMGL: one 6.83–7.20 base; active invalidation 6.80 rather than oldest 4.58 low.
- FTFT: restores 4.03–4.322 first consolidation; adds 3.24–3.30 lower base.
  Selection of the second area still needs quality review, not a blanket pass.
- NCT: removes remote $3–$6 history and subscription-price support. Uses .4218
  thesis failure, but first .4422–.4505 shelf remains too near .452 reference.
- ELMT: uses active opening-base invalidation 20.20 instead of 16.19 origin;
  still returns near 22.10–22.40 shelf and stops upside at 25.03.

The frozen test market packets do not include the Potential Path map at all:
only current quote, breakout observations, and priceAction. Therefore the
prototype cannot use the mapped outer supplement for ELMT from these inputs.
Do not claim mapped coverage was tested or invent substitute map prices.

This is partial improvement, NOT quality acceptance. All raw responses remain
private. No current-card integration, deployment, or publication. Further paid
iteration must recheck remaining daily budget before dispatch.

## Chart-first lean prototype — owner follow-up

Owner raised today's testing ceiling to $10 and authorized removing the separate
momentum section. Visible prototype now has brief setup, pullbacks, upside and
invalidation. `selectionAudit` is internal only and must not render in a card.

Changed input to raw OHLCV plus factual summaries: remove synthetic candidate,
impulse and breakout menus from this prototype request, not from the original
diagnostic or live generator. Historical sharp moves remain real observations;
no assumption of reverse split or data error, no price adjustment or deletion.
The prompt asks for the nearby shelf's role before selecting a meaningful reset.

Five-symbol comparison completed; FTFT's first 10K response was incomplete
(9,105 reasoning tokens) and was explicitly retested once at 16K. Its retry
completed; this does not prove 16K is always required. Other four completed at
10K. All five selected one pullback in this run:

| Symbol | Pullback | Upside areas | Thesis invalidation |
| --- | --- | --- | --- |
| SOAR | .225–.233 | .254–.265; .309–.343 | .214 |
| BMGL | 6.90–7.20 | 7.50–7.70; 7.95–8.22; 8.68–9.73 | 6.83 |
| FTFT | 4.30–4.55 | 5.15–5.25; 5.28–5.76; 6.04–6.28; 6.96–7.48 | 3.69 |
| NCT | .422–.437 | .458–.480; .500–.530; .5557–.5682 | .4218 |
| ELMT | 20.90–21.50 | 23.43–24.39; 24.66–25.03 | 20.22 |

Positive: all five classified the nearby shelf as momentum context rather than
selecting it as the dip-buy area. This is one completed comparison, not proven
repeatability. Remaining issues: wide grouped zones in some reads; SOAR attributes
prior spikes to AI-related news without established historical catalyst evidence;
NCT still calls the post-opening-range .48 high the regular-session high despite
the .5098 opening range. Fix the summary ambiguity rather than only repeat prose
instructions. Outer-map supplement is not tested because these saved requests
contain no frozen Potential Path map; NCT/ELMT therefore lack +30% coverage here.

Batch including incomplete FTFT and retry: estimated $0.3839466. Combined known
daily cost $4.4613521 plus earlier unpriced hosted timeout. $10 guard active only
for private testing; no live application budget setting changed. Three focused
tests and diff check passed. Current analysis/card/settings remain untouched.
This is a preserved test variant, not a deployment candidate or owner acceptance.

See [existing repair and paid evidence](watchlist-deep-setup-generation-repair.md).

## Upside coverage correction — 2026-09-14 (in verification)

Owner clarified: preserve useful nearer levels and include available meaningful
resistance 30% or farther out. Thirty percent is not a ceiling. When historical
resistance does not exist above price, explain that briefly instead of inventing
rounded resistance. Preserve the separate prototype/current-card boundary.

Root cause found: the levels extension engine can synthesize $2.50 increments
above $25. Generic frozen-map audit records lost that provenance; the earlier
prototype mistakenly described them as observed resistance. ELMT's $27.50/$30
therefore must not be treated as candle-backed levels based on that record.

Prototype packet and deterministic supplement now exclude generic map entries
unless supplied candle highs corroborate them, including daily, 4h, 5m and 1m.
Raw history remains available for AI to select actual areas, including areas
not in the supplement. This filter does not reject an analysis or remove its
AI-generated sections. It only prevents unsupported deterministic additions.
The supplement retains original intermediate areas and appends at most one
corroborated farther level. No price is generated to satisfy a percentage.

Prompt now requests concise wording, broader 30%-plus coverage where supported,
and a short highest-area explanation when no higher resistance is identified.
Do not claim all-time highs from a bounded historical sample. Seven focused
checks pass; five-symbol private paid replay is underway. No live-card change,
publication, Discord action or deployment. Help has no user-visible change to
document until the separate prototype is accepted for integration.

Saved FTFT packet: reference $4.90, 80 daily bars. It contains historical areas
above $7 and into $9–$11; the reported later $9.48 high must not be inserted as
though known at generation. Saved ELMT packet: reference $22.52, 98 daily bars;
highest supplied daily high $22.51, intraday high $25.03. First corrected ELMT
paid replay has no $27.50/$30; final upside explanation states that no higher
resistance is identified in the supplied history above $25.03.

All five first-pass calls completed. ELMT omitted fabricated extensions; BMGL
retained intermediate areas and farther historical resistance. Review found
FTFT grouping $10.08–$12.96 too broadly, SOAR appending an excessive historical
inventory to its last explanation, and NCT adding an impractical $4-plus
pre-collapse area to a cents-priced read. Do not call that batch full acceptance.
Tightened compact-area/representative-price selection, explanation length, and
coherent active-regime continuation. Targeted paid rerun of those three only;
original raw responses retained. This changes instructions, not saved market
facts, and does not introduce whole-analysis rejection or automatic extra calls.

### Correction checkpoint

Eight focused tests pass, including rejection of uncorroborated synthetic map
additions, preservation of 4h-backed levels, intermediate-area preservation and
coverage beyond 30%. All five paid replays completed, followed by the three
targeted reruns; no incomplete response in these eight requests.

Final targeted upside outputs:
- FTFT ($4.90 reference): $5.04–$5.25, $5.64–$5.76, $6.24–$6.28, $6.96.
  This is roughly 42% coverage, not coverage of the entire owner-reported $9.48
  later run. Do not claim the test forecast that high.
- NCT: $0.48, $0.505–$0.51, $0.5682; no impractical $4-plus addition.
- SOAR: $0.254–$0.259, $0.265, $0.309–$0.313, $0.336–$0.343; no long tail
  inventory in the last explanation.
- ELMT first corrected replay: no $27.50/$30; $25.03 upper evidenced area with
  an explicit no-higher-resistance-in-supplied-history explanation.
- BMGL first corrected replay preserved nearer areas and farther $9.48–$9.96
  and $10.60–$11.093 historical areas.

This completes the isolated synthetic-upside/provenance correction, not overall
analysis quality acceptance or deployment. Remaining QA includes SOAR's setup
phrase 'into the close' on a midday packet, verbose overview/rule language, and
repeatability of compact area selection. Do not imply every part of these reads
passed just because the upside correction did. Existing full analysis/card,
published reads, settings and Discord remain unchanged.

This checkpoint's eight paid calls: estimated $0.5342692. Known daily total
$5.2903643 plus the earlier unpriced hosted timeout; conservative accounting
with hosted reserve $5.5577078, below the owner's $10 ceiling. Raw source packets
and outputs remain private/ignored. No more paid calls for this checkpoint.

## Full-packet context and concise interpretation follow-up

Owner confirmed the Levels System provides useful prior daily/4h structure;
the full candle packet is intentional because a levels-only analysis failed.
Do not revert to a levels menu or equate today's intraday high with a historical
Levels System output. Today's 1m/5m analysis complements the historical context.

Provenance correction: the saved audit proves the fallback appended ELMT's
$27.50/$30, but does NOT preserve the original underlying ladder. The synthetic
extension code is a possible origin, not a proven trace of that exact request.
The owner's later displayed ladder ends at $22.50 and contains neither price.
Do not describe the displayed ladder as having generated those values.

Read-only refreshed hosted capture on Sep 14 still returns the same noon-era
generations. All five saved request contracts omit both fourHourBars and
levelsSystem fields entirely; advertised timeframes are 1m, 5m, 1d. Each has
97 five-minute bars beginning at 04:00 Eastern and 60 recent one-minute bars.
Daily counts differ: SOAR 163, BMGL 180, FTFT 80, NCT 27, ELMT 98. These are
not complete four-timeframe replay tests. Current local manager has a 4h fetch
path and builder emits fourHourBars, but its hosted execution is not verified
by these earlier captures. Missing field is not evidence of a provider failure.

Added internal Eastern marketClock at the immutable packet as-of and explicit
per-timeframe count/date coverage. Preserve raw candles and original records;
no fake 4h bars, no live generator/ingest changes. Fixed includesOpeningRange
to report whether supplied opening-range bars actually exist. Simplified
repetitive prompt instructions and separated internal audit wording from
short visible trader explanations. Ten focused tests pass, including summer/
winter Eastern offsets, missing 4h reporting, and source immutability.

One five-symbol paid comparison is underway with unchanged original candles,
under the remaining $10 daily ceiling. This does not replace the outstanding
hosted full-packet/four-hour verification or constitute release acceptance.

### Comparison outcome and retained changes

All five completed, costing an estimated $0.350227. This was NOT a quality pass:
SOAR recognized noon but still exposed internal 30%-coverage wording; BMGL,
NCT and ELMT brought back near-reference first pullbacks; FTFT moved broader
invalidation to the oldest session origin. ELMT also described same-day evidence
as prior-session supply. Shortening structural instructions was not accepted.
Restored the stronger prior chart-selection, sole-meaningful-pullback and
not-mechanically-oldest-low instructions; retained Eastern clock, explicit
coverage and internal-versus-visible language separation. Ten focused tests
pass after restoration. Final restored combination is not claimed to have
passed another paid five-symbol batch. Preserve all unsuccessful raw responses
for comparison instead of hiding them or treating HTTP success as quality.

No further paid prompt-only testing this checkpoint. Known daily spend now
$5.6405913 plus the earlier unpriced hosted timeout; conservative reserve total
$5.9079348. Current full analysis/card and hosted runtime remain unchanged.

A proposed private historical-capture helper using the existing EODHD provider
adapter was rejected by the credential-access safety reviewer BEFORE creation
or execution. No EODHD credential was retrieved and no provider request was
sent. The reviewer requires specific owner approval to use the hosted EODHD
credential for historical four-hour fetches. Do not bypass that restriction.
The bounded next test would fetch only SOAR/BMGL/FTFT/NCT/ELMT prior-session
four-hour candles ending before Sep 14, preserve originals, and compare the
enriched packet; it would not publish, edit settings, or send Discord posts.
Missing four-hour context is an untested input boundary, not a promise that
adding it alone will resolve interpretation quality.

## Seven-symbol four-timeframe comparison

Owner explicitly authorized EODHD access, kept hosting migration out of scope,
and added VEEA/SLGB with a clarification that their runs began post-market.
Read-only production configuration confirms Watchlist and Stock Levels use the
staging-named runtime. Its current compiled packet builder lacks fourHourBars;
its file hash matches the saved requests. This is a current implementation
difference from local source, not an EODHD outage or missing daily history.

Retrieved historical 4h candles through the existing EODHD adapter for the five
original cases, ending before Sep 14. Captures contain SOAR 128, BMGL 93, FTFT
127, NCT 128, ELMT 128 bars. Added a replay-only attachment that checks symbol,
cutoff, chronological order, OHLCV validity and completed-bar boundary while
preserving original daily/1m/5m data. Eleven focused checks pass.

VEEA and SLGB are separate newly captured post-market price-action cases at
17:18 Eastern, not relabeled midday originals. VEEA uses Moomoo (798 raw 1m,
159 5m); SLGB's Moomoo bridge was unavailable and Yahoo returned 152 raw 1m,
65 5m. Both have EODHD daily/4h history; SLGB's after-hours volume is unavailable
and the packet correctly marks it null/unavailable, not confirmed zero trading.
No article is supplied for these two price-action-only tests. Neither ticker
was published to the Watchlist or Discord by these test helpers.

Pre-dispatch packet checks passed for all seven: original five retain 60 recent
1m and 97 full-session 5m bars; new post-market cases have 120 recent 1m plus
full available same-day 5m data. Request JSON sizes range from 86 KB to 156 KB.
The serial Luna comparison remains inside the owner-approved $10 daily guard.
Successful responses must still pass manual structural review; do not count
HTTP completion as analysis-quality acceptance. Hosting/current card unchanged.

The initial seven-symbol comparison and the subsequent revised-prompt matched
Luna/Terra comparisons are complete. See the linked progress record for exact
results, residual interpretation limitations, 14 focused test passes, private
artifact handling, cost reconciliation and the still-unaccepted live-format
integration boundary. The owner was asked whether the next-day requirement
supersedes the earlier isolated-prototype-only direction by adding a selectable
Simple analysis option while preserving Current analysis and owner approval.
